# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: azurecosmosdb-mcptoolkit
metadata:
    template: mcptoolkit@1.20.3

# Infrastructure only - no services to deploy
# Use the PowerShell deployment script (Deploy-Cosmos-MCP-Toolkit.ps1) to deploy the application

# Infrastructure parameters
infra:
    provider: bicep
    path: infrastructure
    module: main

